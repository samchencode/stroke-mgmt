import type { TextStyle } from 'react-native';

export const fonts: { [key: string]: TextStyle } = {
  labelLarge: {
    lineHeight: 20,
    fontSize: 14,
    fontWeight: '500',
  },
  bodyLarge: {
    lineHeight: 24,
    fontSize: 16,
    fontWeight: '400',
  },
};
